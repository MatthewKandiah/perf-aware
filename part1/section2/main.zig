// input: binary instruction file containing different variants of mov instruction
// output: decoded instructions, which can be assembled into binary instructions exactly matching input
const std = @import("std");

pub fn main() !void {
    std.debug.print("It works\n", .{});
}
